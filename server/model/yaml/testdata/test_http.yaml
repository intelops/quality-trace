type: Test
spec:
  id: ZsoMdf44R
  name: Get example
  trigger:
    type: http
    httpRequest:
      url: http://test.com/list
      method: GET
  specs:
  - selector: span[name = "Qualitytrace trigger"]
    assertions:
    - quality-trace.response.status = 200
