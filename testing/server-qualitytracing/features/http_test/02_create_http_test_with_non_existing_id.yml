type: Test
spec:
  id: oCkHevQ4R-
  name: Create HTTP Test with non-existing ID
  description: Test step of 'HTTP Test Feature - Test Suite'
  trigger:
    type: http
    httpRequest:
      url: ${var:TARGET_URL}/api/tests
      method: POST
      headers:
      - key: Content-Type
        value: application/json
      body: |
        {
          "type": "Test",
          "spec": {
            "id": "${var:EXAMPLE_TEST_ID}",
            "name": "Pokemon - List - Get a Pokemon",
            "trigger": {
              "type": "http",
              "httpRequest": {
                "url": "${var:DEMO_APP_URL}/pokemon?take=20&skip=0",
                "method": "GET",
                "body": "",
                "headers": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ]
              }
            }
          }
        }
  specs:
  - selector: span[name = "Qualitytrace trigger"]
    assertions:
    - attr:quality-trace.selected_spans.count = 1
    - attr:quality-trace.response.status = 201
    - attr:quality-trace.response.body | json_path '$.spec.id' = var:EXAMPLE_TEST_ID
  - selector: span[name="POST /api/tests" quality-trace.span.type="http"]
    assertions:
    - attr:quality-trace.selected_spans.count = 1
