type: Test
spec:
  id: TjkNevQ4Rs
  name: Run HTTP Test
  description: Test step of 'HTTP Test Feature - Test Suite'
  trigger:
    type: http
    httpRequest:
      url: ${var:TARGET_URL}/api/tests/${var:HTTP_TEST_ID}/run
      method: POST
      headers:
      - key: Content-Type
        value: application/json
      body: '{}'
  specs:
  - selector: span[name = "Qualitytrace trigger"]
    assertions:
    - attr:quality-trace.selected_spans.count = 1
    - attr:quality-trace.response.status = 200
    # Ensure startTime and endTime are present in span
    - attr:quality-trace.span.start_time > 0
    - attr:quality-trace.span.end_time > 0
  - selector: span[name = "POST /api/tests/{testId}/run" quality-trace.span.type = "http"]
    assertions:
    - attr:quality-trace.selected_spans.count = 1
  - selector: span[name = "Trigger test"]
    assertions:
    - attr:quality-trace.selected_spans.count = 1
    - attr:quality-trace.run.trigger.test_id = "${var:HTTP_TEST_ID}"
    - attr:quality-trace.run.trigger.type = "http"
    - attr:quality-trace.run.trigger.http.response_code = 200
  - selector: span[name = "Fetch trace"]
    assertions:
    - attr:quality-trace.selected_spans.count > 0
    - attr:quality-trace.run.trace_poller.test_id = "${var:HTTP_TEST_ID}"
  - selector: span[name = "Fetch trace"]:last
    assertions:
    - attr:quality-trace.run.trace_poller.succesful = "true"
  - selector: span[name = "Execute assertions"]
    assertions:
    - attr:quality-trace.selected_spans.count = 1
    - attr:quality-trace.run.assertion_runner.all_assertions_passed = "true"
  outputs:
  - name: HTTP_TEST_RUN_ID
    selector: span[name = "Qualitytrace trigger"]
    value: attr:quality-trace.response.body | json_path '$.id'
