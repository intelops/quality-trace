apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quality-trace.fullname" . }}-test-connection"
  labels:
    {{- include "quality-trace.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quality-trace.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
